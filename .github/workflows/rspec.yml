name: Run RSpec

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.0.0 # replace with your Ruby version

    - name: Install dependencies
      run: bundle install

    - name: Run RSpec
      run: bundle exec rspec
